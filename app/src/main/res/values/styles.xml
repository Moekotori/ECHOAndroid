<resources>
    <style name="EchoAndroidBaseTheme" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:fontFamily">@font/outfit</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.EchoAndroid" parent="@style/EchoAndroidBaseTheme" />

    <style name="Theme.EchoAndroid.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/echo_splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/ic_launcher_background</item>
        <item name="postSplashScreenTheme">@style/Theme.EchoAndroid</item>
    </style>
</resources>
